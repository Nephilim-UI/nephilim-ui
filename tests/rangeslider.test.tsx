import React from 'react'

import { render } from '@testing-library/react'
import { RangeSlider } from '../src'

describe('RangeSlider rendering', () => {
  test('Render/RangeSlider', () => {
    render(<RangeSlider />)
  })
})
